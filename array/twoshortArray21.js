var findMedianSortedArrays = function (nums1, nums2) { 
    const arr=[...nums1,...nums2];
     const l=arr.length;
     const mid=Math.round(l/2).toFixed(5)

     
    return mid
    }
    console.log(findMedianSortedArrays([1,2],[3,4]));
